# SnapBug TODO

- Verify invite link behavior. The generated invite link should open the pending invite flow directly; currently pasting the link still appears to require manually pressing invite/accept and the link itself may not be doing enough.
- Add attachment deletion. Only the user who uploaded an attachment should be allowed to delete it, and the delete operation must be written to the audit log.
- Add a user profile menu, including account/password management.
- Improve screenshot clarity in the SDK widget capture flow.
- Make the SDK a proper documented SDK, including clear installation and integration instructions.
- Update the test client with current keys/config patterns and the latest SDK behavior.
